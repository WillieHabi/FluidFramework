{
	"name": "Sort by priority",
	"generatedCode": [
		"// Reorganize the workItems array to sort by priority, with 'critical' first followed by 'high', 'medium', and 'low'.\nconst priorityOrder = ['critical', 'high', 'medium', 'low'];\n\ncontext.root.workItems.sort((a, b) => {\n  return priorityOrder.indexOf(a.priority) - priorityOrder.indexOf(b.priority);\n});",
		"// Since context.root.workItems is not a standard array that supports .sort(),\n// We need to implement a manual sorting method that uses remove and insert operations.\n\nconst priorityOrder = ['critical', 'high', 'medium', 'low'];\nconst workItemsArray = Array.from(context.root.workItems);\n\nworkItemsArray.sort((a, b) => {\n    return priorityOrder.indexOf(a.priority) - priorityOrder.indexOf(b.priority);\n});\n\n// Clear the original array and re-insert sorted items.\ncontext.root.workItems.removeRange();\nworkItemsArray.forEach((item, index) => {\n    context.root.workItems.insertAt(index, item);\n});",
		"// To resolve the error of inserting the same node, we need to recreate each WorkItem during sorting and reinsertion.\n\n// Define the order of priorities\nconst priorityOrder = ['critical', 'high', 'medium', 'low'];\n\n// Extract, sort by priority, and reinsert work items\nconst workItemsArray = Array.from(context.root.workItems);\n\n// Sort work items by their priority\nworkItemsArray.sort((a, b) => priorityOrder.indexOf(a.priority) - priorityOrder.indexOf(b.priority));\n\n// Clear the original work items array\ncontext.root.workItems.removeRange();\n\n// Recreate and insert sorted work items\nworkItemsArray.forEach(item => {\n    const newItem = context.create.WorkItem({\n        title: item.title,\n        description: item.description,\n        status: item.status,\n        priority: item.priority,\n        assignee: item.assignee,\n        storyPoints: item.storyPoints\n    });\n    context.root.workItems.insertAt(context.root.workItems.length, newItem);\n});\n"
	]
}